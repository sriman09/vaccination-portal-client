import { IVaccination } from "./vaccination.model";

export interface IPaitent{
    name: string;
    dateOfBirth: string;
    gender: string;
    placeOfBirth: string;
    bloodGroup: string;
    height: number;
    weight: number;
}